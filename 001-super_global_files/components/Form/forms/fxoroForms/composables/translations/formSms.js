import translationsGlossary from "../../../../../../composables/translationsGlossary";

const formSMS = () => {
  const smsProcessingSameLead = {
    en: "Processing the same lead",
    it: "Processing the same lead",
    tr: "Processing the same lead",
    ro: "Processing the same lead",
    hu: "Processing the same lead",
    ar: "Processing the same lead",
    de: "Processing the same lead",
    es: "Processing the same lead",
    sv: "Processing the same lead",
    pt: "Processing the same lead",
    fi: "Processing the same lead",
    pl: "Processing the same lead",
    th: "Processing the same lead",
    ms: "Processing the same lead",
  };

  return {
    smsProcessingSameLead,
  };
};

export default formSMS;
